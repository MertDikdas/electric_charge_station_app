import 'package:flutter/material.dart';

class StationManagerPanelScreen extends StatelessWidget {
  const StationManagerPanelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Station Manager Panel')));
  }
}
