import 'package:flutter/material.dart';

class StationOperatorPanelScreen extends StatelessWidget {
  const StationOperatorPanelScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Station Operator Panel')));
  }
}
